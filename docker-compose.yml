services:
  dcf-50:
    build: .
    container_name: dcf-50
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/data
    mem_limit: 512m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
